[package]
edition = "2024"
name = "smux_v1"
version = "0.1.0"

description = "流复用协议第一版"

[dependencies]
abs_buff = { path = "../abs_buff" }
abs_art-bridge = { version = "0.2.2" }

crc = { version = "3.4.0" }
